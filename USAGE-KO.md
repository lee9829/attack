# 자사 검색 노출 · CTA 점검 사용법 (v1.9)

## 목적 (이 범위만)

| 항목 | 내용 |
|------|------|
| 도메인 | **자사 최대 약 300개** (`domains.txt` 또는 목록 붙여넣기) |
| 검색어 | **여러 개** (`keywords.txt` 또는 목록) |
| 행위 | 검색 → **자사 도메인 유기 결과 여러 건** 클릭 → 스크롤·체류 → CTA |
| 목적 | 다수 자사 사이트 검색 노출·CTA 점검 |
| 금지 | 광고 클릭 · 목록에 없는 타인 사이트 |

---

## 실행

1. Octo Browser 실행·로그인  
2. `start.vbs` 더블클릭  
3. **홈** → API 토큰 → 연결 테스트  
4. **프록시** → 목록 검증  
5. **프로필** → 프로필 이름  
6. **검색·점검** → 검색어 / 자사 도메인 / CTA  
7. **베타 테스트** 탭에서 T1~T9 단계별 확인 (권장)  
8. **▶ 시작** (먼저 DRY RUN 권장)

## 베타 단계 테스트 (의견 수집)

왼쪽 **베타 테스트** 메뉴:

| 단계 | 내용 | 브라우저 |
|------|------|----------|
| T1 | Cloud API | 없음 |
| T2 | Local API | 없음 |
| T3 | 프록시 파싱 | 없음 |
| T4 | 프로필+프록시 주입 | 없음 |
| T5 | 프로필 Start + CDP + IP | 있음(짧게) |
| T6 | Google 로그인+2FA | 있음 |
| T7 | 검색·자사 1클릭 | 있음 |
| T7+ | 검색 3클릭 | 있음 |
| T8 | 자사 URL CTA | 있음 |
| T9 | DRY RUN 배정 | 없음 |

각 단계 후 **평가(좋음/보통/문제있음) + 의견 저장** → `feedback/` 폴더

---

## 검색·점검 권장 값 (다수 사이트)

| 항목 | 값 |
|------|-----|
| 자사 도메인 목록 | 한 줄에 하나 · 300개 · `domains.txt` |
| 추가 검색어 목록 | 한 줄에 하나 · `keywords.txt` |
| 검색어당 클릭 수 | `5`~`10` (결과 여러 개 클릭) |
| SERP 페이지 | `5` (더 많이 스캔) |
| URL 정규식 | 여러 줄 가능 (OR 결합) |
| 자사 도메인만 | **ON** |
| 광고 클릭 안 함 | **ON** |
| CTA 글자 | `메뉴`, `예약` 등 |

### domains.txt 예
```text
brand1.com
shop.brand1.com
brand2.co.kr
```

### keywords.txt 예
```text
brand1 공식
brand2 예약
우리브랜드
```

---

## 동작 요약

```
Octo 프로필 + 프록시
  → Google 검색
  → 광고/타인 사이트 제외
  → 자사 도메인 결과만 클릭
  → 스크롤·체류
  → 메뉴 / 예약 등 CTA
  → (선택) 재방문
  → 프로필 종료
```

로그: 프로필 · 프록시 IP · 검색어 · 클릭 URL · CTA  
→ 화면 콘솔 + `logs/session_*.log`
